# Archived probes

These added-mass and base-velocity probe scripts are retained for reference,
but are not part of the active evaluation workflow. Their former unit tests are
kept under `tests/` in this directory so normal test discovery does not run
them.
